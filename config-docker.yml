server:
  host: 0.0.0.0
  port: 8080

grpc:
  port: 9090

db:
  uri: jdbc:postgresql://postgres:5432/kotlinvertx
  user: username
  password: password

gcp:
  project: local-run
  pubsub:
    emulator:
      enabled: true
      address: gcp-pubsub:8538
