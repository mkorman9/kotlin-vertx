buildscript {
    ext {
        kotlinVersion = '1.5.31'
        vertxVersion = '4.2.7'
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
        classpath "org.jetbrains.kotlin:kotlin-noarg:$kotlinVersion"
    }
}

plugins {
    id 'java'
    id 'application'
    id 'org.jetbrains.kotlin.jvm' version '1.3.71'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
}

apply plugin: 'kotlin'
apply plugin: "kotlin-jpa"

group 'org.example'
version '1.0-SNAPSHOT'

def mainVerticleName = 'com.github.mkorman9.vertx.MainVerticle'

repositories {
    mavenCentral()
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlinVersion"

    implementation "io.vertx:vertx-core:$vertxVersion"
    implementation "io.vertx:vertx-web:$vertxVersion"
    implementation "io.vertx:vertx-lang-kotlin:$vertxVersion"

    implementation 'org.slf4j:slf4j-log4j12:1.7.36'
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.13.2.2'
    implementation 'org.hibernate.reactive:hibernate-reactive-core:1.1.4.Final'
    implementation 'io.smallrye.reactive:smallrye-mutiny-vertx-core:2.21.0'
    implementation 'io.smallrye.reactive:smallrye-mutiny-vertx-web-client:2.21.0'
    implementation 'io.smallrye.reactive:smallrye-mutiny-vertx-pg-client:2.21.0'

    annotationProcessor 'org.hibernate.orm:hibernate-jpamodelgen:6.0.0.Final'
}

shadowJar {
    archiveBaseName = 'app'
    archiveClassifier = 'shadow'

    manifest {
        attributes 'Main-Verticle': mainVerticleName
    }
    mergeServiceFiles {
        include 'META-INF/services/io.vertx.core.spi.VerticleFactory'
    }
}
